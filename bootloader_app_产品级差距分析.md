# Bootloader + App 项目：学习级 vs 产品级差距分析与改动清单

> 以 Bootloader + App（基于 RT-Thread qboot 软件包）为例
> 目的：说清个人学习项目和真实产品的具体差距，给出可执行的改动清单

---

## 一、项目当前状态（学习级）

```
当前实现：
├── qboot 软件包（RT-Thread 的 OTA 软件包）
├── App 固件（业务程序）
├── Bootloader（qboot 提供，启动 + 分区切换）
├── OTA 下载（Webclient / HTTP）
└── 双分区（A/B 交替升级）
```

**现在能跑通的**：
- 正常下载固件 → 写入备用分区 → 校验 → 切换分区 → 启动新固件
- demo 演示升级成功

**没做的**（下面逐项展开）：
- 断电恢复、升级失败处理、固件签名校验、版本管理、回滚策略、产线烧录、灰度发布、故障统计

---

## 二、逐维度对比：学习级 vs 产品级

### 维度 1：固件校验

| | 学习级（当前） | 产品级 |
|---|---|---|
| 校验方式 | CRC32 校验（qboot 自带） | CRC32 + 签名验签（RSA/ECDSA） |
| 防篡改 | 无 | 固件被篡改/替换时拒绝升级 |
| 密钥管理 | 无 | 私钥离线保管，公钥编译进 Bootloader |

**差距**：CRC 只能防传输错误，不能防恶意篡改。产品出货后有人截获 OTA 推送替换固件，CRC 校验过得了，签名验验不过。

**怎么改**：
```
固件包格式（产品级）：
┌──────────────────────────┐
│  Header（版本号/大小/时间戳）  │
├──────────────────────────┤
│  固件二进制               │
├──────────────────────────┤
│  CRC32                   │
├──────────────────────────┤
│  RSA 签名（对 Header+固件+CRC 签名）│
└──────────────────────────┘

Bootloader 验证流程：
1. CRC32 校验 → 不过直接拒绝
2. RSA 验签 → 用编译进的公钥验签名，不过拒绝
3. 两层都过才写 Flash
```

**代码层面要加的**：
- 固件打包工具（PC 端 Python 脚本，用私钥签名）
- Bootloader 里加 RSA/ECDSA 验签（mbedTLS 提供，你已经在用）
- 公钥用 `const unsigned char[]` 编译进 Bootloader，私钥不进设备

---

### 维度 2：断电恢复

| | 学习级（当前） | 产品级 |
|---|---|---|
| 下载中断电 | 重新下载，可能写坏分区 | 能恢复或安全回滚 |
| 写 Flash 中断电 | 分区数据不完整 | 检测到不完整，放弃该分区 |
| 切换分区后断电 | 可能启动到坏固件 | 启动计数 + 失败回滚 |

**差距**：学习项目升级断电就废了，产品要做到"断电任意时刻，上电后都能恢复到可用状态"。

**怎么改**：

```
断电安全状态机（产品级）：

阶段 1: 下载中
  → 每下载 4KB 写 Flash + 记录已下载偏移到备份寄存器
  → 断电上电后：读偏移，从断点续传

阶段 2: 下载完成，校验中
  → 校验通过：标记分区状态 = "READY"（写 Flash magic 字）
  → 校验失败：标记分区状态 = "INVALID"
  → 断电上电后：读状态，READY 才切换，INVALID 直接放弃

阶段 3: 切换分区
  → 先写启动标志 = "TRY_NEW" 到备份寄存器
  → 再改分区表指向新分区
  → 断电上电后：读标志，TRY_NEW 则启动新分区但不算成功

阶段 4: 新固件启动成功
  → App 运行后主动写标志 = "CONFIRMED"
  → 断电上电后：CONFIRMED 才算升级成功

阶段 5: 新固件启动失败
  → Bootloader 计数启动次数
  → 连续 3 次没收到 CONFIRMED → 回滚到旧分区
```

**代码层面要加的**：
- 分区状态字段（INVALID / DOWNLOADING / READY / TRY_NEW / CONFIRMED）
- 备份寄存器存升级状态（断电不丢）
- App 启动后主动调 `qboot_confirm()` 确认升级成功
- Bootloader 启动计数 + 回滚逻辑

---

### 维度 3：版本管理

| | 学习级（当前） | 产品级 |
|---|---|---|
| 版本号 | 没有或手写 | 固件 Header 里固定格式 |
| 版本检查 | 无 | 不允许降级（防回滚攻击） |
| 版本追溯 | 无 | 云端知道每台设备跑什么版本 |

**差距**：学习项目没有版本号概念，产品要管"每台设备什么版本""能不能从 v1.2 直接升到 v1.5""不允许降级到有漏洞的旧版"。

**怎么改**：

```
版本号格式：major.minor.patch.build
  例：1.2.3.1024

固件 Header 里加：
struct firmware_header {
    uint32_t magic;          // 0xDEADBEEF
    uint16_t hw_version;     // 硬件版本，防止跨硬件升级
    uint16_t fw_major;
    uint16_t fw_minor;
    uint16_t fw_patch;
    uint32_t build_number;   // 单调递增，用于防降级
    uint32_t firmware_size;
    uint32_t crc32;
    uint8_t  signature[256]; // RSA 签名
};

升级前检查：
1. hw_version 匹配？不匹配拒绝（防止 H743 的固件刷到 F407）
2. build_number > 当前？不大于拒绝（防降级）
3. CRC + 签名校验
```

**代码层面要加的**：
- 固件 Header 结构体定义
- 编译时自动注入版本号（CMake/Makefile 脚本从 git tag 生成）
- Bootloader 升级前检查版本号
- App 上报当前版本号到云端（MQTT 属性上报）

---

### 维度 4：回滚策略

| | 学习级（当前） | 产品级 |
|---|---|---|
| 回滚触发 | 无 | 启动失败计数 / App 主动请求回滚 |
| 回滚后 | 无 | 旧分区还在，能正常跑 |
| 回滚上报 | 无 | 云端记录"某设备回滚了"，告警 |

**差距**：qboot 可能支持基本的双分区切换，但"新固件启动失败自动回滚"这个逻辑要自己加。

**怎么改**：

```
Bootloader 启动流程（产品级）：

1. 读备份寄存器：启动标志 + 启动计数
2. 如果标志 = TRY_NEW：
     启动计数++
     if 启动计数 >= 3:
         标志 = ROLLBACK
         切换回旧分区
         上报"启动失败回滚"到备份寄存器（App 上电后上报云端）
     else:
         启动新分区
3. 如果标志 = CONFIRMED：
     正常启动当前分区
4. 如果标志 = ROLLBACK：
     启动旧分区，等下一次 OTA
```

**App 侧要加的**：
```c
// App 启动成功后（比如跑 30 秒没死，或关键功能初始化通过）
void app_boot_confirm(void) {
    // 写 CONFIRMED 到备份寄存器
    rt_hw_bootloader_confirm();
    // 上报云端当前版本
    mqtt_report_property("fw_version", FW_VERSION_STRING);
}
```

---

### 维度 5：产线烧录

| | 学习级（当前） | 产品级 |
|---|---|---|
| 烧录方式 | J-Link 一块一块烧 | 产线批量烧录 |
| 唯一标识 | 无 | 每台设备唯一 MAC / 序号 / 设备证书 |
| 烧录校验 | 无 | 烧录后自检，过才出厂 |

**差距**：学习项目一块板子 J-Link 烧就完了，产品 1000 块板子产线怎么烧、每台唯一标识怎么注入、烧录不良怎么处理——这些 qboot 完全不管。

**怎么改**：

```
产线烧录流程（产品级）：

1. 产线 PC 连接烧录器（USB → SWD）
2. 烧录 Bootloader（所有设备相同）
3. 烧录 App 基础固件（所有设备相同）
4. 注入唯一标识：
   - 生成唯一 MAC 地址（或从芯片 UID 派生）
   - 生成设备序号（产线流水号）
   - 生成设备证书（从 CA 签发，私钥注入设备）
   - 写入 Flash 指定区域 / OTP 熔丝
5. 自检：
   - 读回校验标识写入正确
   - 启动设备，串口输出 "OK" 才算过
   - 不过标记不良，产线隔离
6. 老化 4~8 小时，老化后复测
```

**代码层面要加的**：
- 产线烧录工具（PC 端，批量调用 OpenOCD/ST-Link Utility）
- 唯一标识注入区（Flash 固定地址或 OTP）
- 设备首次启动自检逻辑
- 产线串口输出自检结果

---

### 维度 6：灰度发布

| | 学习级（当前） | 产品级 |
|---|---|---|
| 推送范围 | 全部 | 灰度 5% → 20% → 100% |
| 推送策略 | 无 | 按设备 ID hash / 地域 / 版本号筛选 |
| 失败止损 | 无 | 灰度期间故障率超阈值自动停推 |

**差距**：学习项目 OTA 推了就全推，产品要先灰度 5% 观察故障率，没问题再扩大。

**怎么改**（云端侧，不是设备侧）：

```
云端灰度发布流程（产品级）：

1. 上传新固件到云端，标记灰度比例 5%
2. 设备上报 OTA 请求时，云端按设备 ID hash 判断是否在灰度范围
3. 灰度设备升级后，云端监控：
   - 升级成功率
   - 升级后复位次数 / HardFault 次数
   - 升级后心跳是否正常
4. 故障率 < 阈值 → 扩大灰度到 20% → 50% → 100%
5. 故障率 >= 阈值 → 停止推送，已升级的设备回滚
```

**设备侧要配合的**：
- OTA 请求带设备 ID + 当前版本号
- 升级结果上报（成功/失败/原因）
- 升级后复位次数和异常上报（上一轮面试题讨论的机制）

---

### 维度 7：故障统计与远程诊断

| | 学习级（当前） | 产品级 |
|---|---|---|
| 升级失败 | 串口打日志 | 上报云端，按版本/设备聚合 |
| 启动失败 | 无 | Bootloader 记数 + 上报 |
| 分区状态 | 无 | 云端能看到每台设备分区状态 |

**差距**：学习项目升级失败了串口看看日志就完了，产品要上报云端做统计，知道"v1.3 在 1000 台设备上升级失败率 2%，失败原因集中在 Flash 写超时"。

**怎么改**：

```
设备上报内容（产品级）：

OTA 结果上报：
{
  "device_id": "xxx",
  "event": "ota_result",
  "from_version": "1.2.3",
  "to_version": "1.3.0",
  "result": "success" / "failed",
  "fail_reason": "crc_error" / "flash_timeout" / "sign_error",
  "duration_sec": 45
}

启动失败上报（Bootloader 存到备份寄存器，App 上电后上报）：
{
  "device_id": "xxx",
  "event": "boot_fail",
  "fail_count": 2,
  "last_pc": "0x0804A2F0",
  "partition": "B"
}

分区状态上报：
{
  "device_id": "xxx",
  "active_partition": "A",
  "backup_partition": "B",
  "backup_status": "READY" / "INVALID" / "DOWNLOADING"
}
```

---

## 三、改动优先级

按"面试能讲的价值"和"实现成本"排优先级：

| 优先级 | 改动项 | 成本 | 面试能讲什么 |
|---|---|---|---|
| P0 | 断电恢复状态机 | 中（2-3 天） | "升级任意时刻断电都能恢复" |
| P0 | 启动失败计数 + 回滚 | 低（1 天） | "新固件启动 3 次失败自动回滚" |
| P1 | 固件 Header + 版本管理 | 低（1 天） | "防降级、防跨硬件刷机" |
| P1 | App 主动 confirm + 版本上报 | 低（半天） | "App 跑稳了才确认升级" |
| P2 | CRC + RSA 签名验签 | 中（2-3 天） | "防篡改，公钥编译进 Bootloader" |
| P2 | 升级结果上报云端 | 低（1 天） | "故障率统计，按版本聚合" |
| P3 | 产线烧录工具 | 高（1 周+） | "批量烧录 + 唯一标识注入" |
| P3 | 灰度发布（云端侧） | 高（云端开发） | "5% 灰度 → 故障率监控 → 全量" |

**建议**：先做 P0 + P1（约 1 周），做完后 H743 项目的 OTA 部分就能说"接近产品级"了。P2 有时间再做。P3 需要云端配合，个人项目做不了，但面试时能说出方案就行。

---

## 三点五、优化方案汇总表

| 序号 | 优化项 | 当前状态（学习级） | 目标状态（产品级） | 具体方案 | 涉及代码/模块 | 工时 | 优先级 | 面试能讲 | 个人可做 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 断电恢复状态机 | 断电后重新下载，可能写坏分区 | 任意时刻断电上电后可恢复到可用状态 | 四阶段状态机（下载/校验/切换/确认），每阶段写状态到 RTC 备份寄存器，上电读状态恢复 | Bootloader 状态机 + RTC 备份寄存器读写 + 下载断点续传 | 2-3 天 | P0 | "升级任意时刻断电都能恢复" | ✅ |
| 2 | 启动失败计数 + 自动回滚 | 新固件起不来就卡死 | 连续 3 次启动失败自动回滚旧分区 | Bootloader 读启动计数，未收到 App confirm 计数+1，超阈值切回旧分区 | Bootloader 启动逻辑 + 备份寄存器计数 | 1 天 | P0 | "新固件启动 3 次失败自动回滚" | ✅ |
| 3 | App 主动确认机制 | 无确认，切换即认为成功 | App 跑稳后主动写 CONFIRMED，没确认不算成功 | App 初始化完成 + 关键功能自检通过后调 confirm 写备份寄存器 | App 启动流程 + confirm 接口 | 半天 | P0 | "App 跑稳了才确认升级" | ✅ |
| 4 | 固件 Header + 版本管理 | 无版本号或手写 | Header 含版本号/硬件版本/构建号，防降级防跨硬件 | 定义 firmware_header 结构体，编译时注入版本，升级前检查 build_number 和 hw_version | 固件打包脚本 + Bootloader 升级前检查 | 1 天 | P1 | "防降级、防跨硬件刷机" | ✅ |
| 5 | 分区状态字段 | 无状态或只有有效/无效 | INVALID/DOWNLOADING/READY/TRY_NEW/CONFIRMED 五状态 | 扩展分区表结构，每个分区加 state 字段，Bootloader 按状态决策 | 分区表结构体 + Bootloader 状态判断 | 半天 | P1 | "五状态分区管理" | ✅ |
| 6 | 固件 CRC + RSA 签名验签 | 仅 CRC32 | CRC32 + RSA/ECDSA 签名验签，防篡改 | PC 端打包工具用私钥签名，Bootloader 用编译进的公钥验签 | PC 打包脚本(Python) + Bootloader 验签(mbedTLS) + 公钥常量 | 2-3 天 | P2 | "防篡改，公钥编译进 Bootloader" | ✅ |
| 7 | 升级结果上报云端 | 串口日志 | MQTT 上报升级结果/失败原因/耗时 | OTA 完成或失败后发 MQTT 事件，含 from/to 版本、结果、原因 | App OTA 回调 + MQTT 上报接口 | 1 天 | P2 | "故障率统计，按版本聚合" | ✅ |
| 8 | 启动失败原因上报 | 无 | Bootloader 存失败 PC/计数到备份寄存器，App 上电上报 | Bootloader HardFault Handler 存现场，App 读出后 MQTT 上报 | Bootloader HardFault Handler + App 上电上报 | 1 天 | P2 | "启动失败远程诊断" | ✅ |
| 9 | 分区状态上报 | 无 | 云端能看到每台设备活跃分区/备用分区/备用状态 | App 周期性或 OTA 后上报分区状态 | App MQTT 属性上报 | 半天 | P2 | "远程可见分区状态" | ✅ |
| 10 | 产线批量烧录 | J-Link 单块烧 | 批量烧录 + 唯一标识注入 + 自检 | PC 端烧录工具批量调用 OpenOCD，注入 MAC/序号/证书，烧录后自检 | PC 烧录工具 + 唯一标识注入区 + 自检逻辑 | 1 周+ | P3 | "批量烧录 + 唯一标识注入" | ❌ 需产线 |
| 11 | 唯一标识注入 | 无 | 每台设备唯一 MAC/序号/设备证书 | 烧录时写入 Flash 固定区或 OTP，App 读取用于 MQTT 设备 ID 和 TLS 客户端证书 | 烧录工具 + App 标识读取 | 2-3 天 | P3 | "一机一证" | ❌ 需产线 |
| 12 | 灰度发布 | 全推 | 5% → 20% → 100%，故障率监控自动停推 | 云端按设备 ID hash 筛灰度范围，监控故障率超阈值停推 | 云端 OTA 服务 + 灰度策略配置 | 1 周+ | P3 | "灰度发布 + 故障率止损" | ❌ 需云端 |
| 13 | 老化测试与产线自检 | 无 | 老化 4-8h 后复测，不良品隔离 | 产线烧录后老化，老化后自动复测功能，不过标记不良 | 产线测试工装 + 自检固件 | 1 周+ | P3 | "量产老化与不良率" | ❌ 需产线 |
| 14 | 长跑稳定性验证 | demo 跑通就停 | 7 天连续运行，记录堆/栈/复位/异常 | 长跑测试脚本 + 日志记录 + 内存监控 + 看门狗 | 测试脚本 + App 监控代码 | 2-3 天 | P0 | "7 天长跑稳定无复位" | ✅ |
| 15 | 异常场景处理 | 仅正常路径 | 断网/断电/Flash 写坏/固件损坏/空间不足 | 各异常分支加超时退出 + 错误码 + 日志 + 重试策略 | App OTA 流程各分支 | 1-2 天 | P1 | "断网断电 Flash 异常都有处理" | ✅ |

### 按优先级分组

**P0（必做，约 1 周）**：序号 1, 2, 3, 14
- 断电恢复 + 启动回滚 + App 确认 + 长跑验证
- 做完后：OTA 能说"接近产品级"，有数据有机制

**P1（建议做，约 2-3 天）**：序号 4, 5, 15
- 版本管理 + 分区状态 + 异常处理
- 做完后：面试追问每个环节都有具体答

**P2（有时间做，约 1 周）**：序号 6, 7, 8, 9
- 签名验签 + 上报机制
- 做完后：安全性和远程诊断能力

**P3（做不了，面试说方案）**：序号 10, 11, 12, 13
- 产线 + 灰度 + 老化
- 个人项目无产线环境，面试时说方案和流程即可

### 按个人可做性分组

**个人可做（11 项）**：1, 2, 3, 4, 5, 6, 7, 8, 9, 14, 15
- 全部在开发板上可实现，不依赖产线和云端

**个人做不了（4 项）**：10, 11, 12, 13
- 需要产线环境或云端 OTA 服务，面试时说方案

---

## 四、做完 P0+P1 后简历怎么写

**现在写的**：
> 开发 HTTPS OTA 远程升级与 Bootloader 双分区方案

**做完 P0+P1 后改成**：
> 基于 qboot 软件包实现 A/B 双分区 OTA，增加断电安全状态机（下载/校验/切换/确认四阶段，任意时刻断电可恢复）、新固件启动失败计数与自动回滚（连续 3 次未确认回滚旧分区）、固件版本管理（防降级 + 防跨硬件）、App 主动确认机制。升级结果与启动失败原因通过 MQTT 上报云端。

**差异**：前者一句话，后者有具体机制和参数，面试官能追问每个环节。

---

## 五、面试时怎么讲这个项目

**被问"OTA 怎么做的"**：

> 基于 RT-Thread 的 qboot 软件包做双分区 OTA，qboot 提供基础的分区管理和固件写入，我在上面加了断电安全状态机——下载、校验、切换、确认四个阶段，每个阶段写状态到 RTC 备份寄存器，任意时刻断电上电后读状态恢复。新固件启动后 App 主动调 confirm 确认成功，连续 3 次没确认就回滚旧分区。固件 Header 里带版本号和硬件版本，防止降级和跨硬件刷机。

**被问"qboot 不是现成的吗，你做了什么"**：

> qboot 提供基础的分区切换和固件写入，但断电恢复状态机、启动失败回滚、版本检查这些 qboot 没有，是我加的。qboot 只管"写入 + 切换"，不管"断电了怎么办""新固件起不来怎么办"——这些产品场景的问题是我自己处理的。

**被问"量产怎么烧录"**（P3 没做，诚实说）：

> 产线烧录这块我个人项目没做，因为没有产线环境。我知道方案是批量烧录器 + 唯一标识注入 + 自检，但没实际做过，这块如果进公司我需要学产线流程。